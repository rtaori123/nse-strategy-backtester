"""Drop your own strategy files in this folder.

Every module here is auto-imported at startup (except names starting with ``_``).
Each should build a ``Strategy`` and call ``register(...)`` — see ``_template.py``
for a documented skeleton and ``example_inside_bar.py`` for a working example.
"""
